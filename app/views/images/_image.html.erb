<p>
	<strong>Title:</strong>
	<%= image.title %>
</p>

<%= image_tag image.file.url %>
<p>
  <%= link_to 'Destroy Image', [image.product, image],
               method: :delete,
               data: { confirm: 'Are you sure?' } %>
</p>